# 网络优化

## 启用 BBR + FQ

```shell
# 使用 fq_codel 防 bufferbloat）
tc qdisc replace dev eth0 root fq_codel
# 启用 BBR
sysctl -w net.ipv4.tcp_congestion_control=bbr

# 查看当前拥塞控制算法
sysctl net.ipv4.tcp_congestion_control

# 查看当前 qdisc （网卡换成实际网卡名）
tc qdisc show dev eth0
```
